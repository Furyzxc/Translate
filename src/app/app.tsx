import { Select } from "@/features/select";
import { TranslationBlock } from "@/features/translationBlock";

export const App = () => {
    return (
        <div className='translation'>
            <Select/>
           <TranslationBlock/>
        </div>
    );
}